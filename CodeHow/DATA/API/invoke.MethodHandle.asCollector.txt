invoke.MethodHandle.asCollector(Class<?> arrayType,\n           int arrayLength)\nMakes an array-collecting method handle, which accepts a given number of trailing\n positional arguments and collects them into an array argument.\n