channels.Channels.newWriter(WritableByteChannel ch,\n         String csName)\nConstructs a writer that encodes characters according to the named\n charset and writes the resulting bytes to the given channel.\n