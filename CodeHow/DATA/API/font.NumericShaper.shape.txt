font.NumericShaper.shape(char[] text,\n     int start,\n     int count,\n     NumericShaper.Range context)\nConverts the digits in the text that occur between start and start + count, using the provided context.\n