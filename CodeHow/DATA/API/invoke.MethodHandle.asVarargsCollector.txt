invoke.MethodHandle.asVarargsCollector(Class<?> arrayType)\nMakes a variable arity adapter which is able to accept\n any number of trailing positional arguments and collect them\n into an array argument.\n